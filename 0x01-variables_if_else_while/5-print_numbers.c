#include<stdio.h>

/**
 * main - Entry point
 *
 * Descrption:  prints all single digit numbers of base 10 starting from 0
 * Return: Always 0 on Success
 */
int  main(void)
{
	int i = 0;

	for (i = 0 ; i <= 9; i++)
	{
		printf("%d", i);

	}
	printf('\n');
	return (o);
}
